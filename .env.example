# Keep this file free of real credentials. Set the matching environment variables
# in the current PowerShell session before running the SquareFace image script.
SQUAREFACE_API_KEY=
SQUAREFACE_IMAGE_MODEL=gpt-image-1
SQUAREFACE_IMAGE_ENDPOINT=https://api.squarefaceicon.org/v1/images/generations
